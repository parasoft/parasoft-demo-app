query GetCartItemByItemId($itemId: Long!){
    getCartItemByItemId(itemId: $itemId){
        id
        userId
        itemId
        name
        description
        image
        realInStock
        quantity
    }
}