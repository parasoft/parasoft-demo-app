mutation AddNewItem($itemsDTO: ItemsDTO!) {
    addNewItem(itemsDTO: $itemsDTO) {
        id
        name
        description
        inStock
        image
        region
        lastAccessedDate
        categoryId
    }
}