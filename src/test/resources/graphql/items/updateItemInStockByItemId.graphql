mutation UpdateItemInStockByItemId($itemId: ID!, $newInStock: Int) {
    updateItemInStockByItemId(itemId: $itemId, newInStock: $newInStock) {
        id
        name
        description
        inStock
        image
        region
        lastAccessedDate
        categoryId
    }
}