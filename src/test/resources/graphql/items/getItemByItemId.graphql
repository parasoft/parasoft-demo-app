query GetItemByItemId($itemId: Long!) {
    getItemByItemId(itemId: $itemId) {
        id
        name
        description
        inStock
        image
        region
        lastAccessedDate
        categoryId
    }
}