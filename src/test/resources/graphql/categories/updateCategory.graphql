mutation UpdateCategory($categoryId: Long!, $categoryDto: CategoryDTO!) {
    updateCategory(categoryId: $categoryId, categoryDto: $categoryDto) {
        id
        name
        description
        image
    }
}