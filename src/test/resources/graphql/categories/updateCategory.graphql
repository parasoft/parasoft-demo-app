mutation UpdateCategory($categoryId: ID!, $categoryDto: CategoryDTO!) {
    updateCategory(categoryId: $categoryId, categoryDto: $categoryDto) {
        id
        name
        description
        image
    }
}